# Server listening port (defaults to 3001 if not specified)
PORT=3001

# Allowed client origin for CORS (e.g. http://localhost:5173 for local dev, or https://your-judgement-app.vercel.app for production)
# Defaults to '*' if not specified
CLIENT_URL=http://localhost:5173
